[package]
name = "quickhull"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
authors = ["Joona Aalto <jondolf.dev@gmail.com>"]
description = "Quickhull algorithm for computing convex hulls"
repository = "https://github.com/Jondolf/quickhull"
readme = "README.md"
keywords = ["convex", "hull", "geo", "geometry"]
categories = ["algorithms", "mathematics", "game-development"]

[dependencies]
# Math
glam = "0.32"
glam_matrix_extras = "0.3"
robust = "1.1.0"
approx = "0.5"
thiserror = "2.0"

# Hashing
foldhash = "0.2"
hashbrown = "0.17"

[dev-dependencies]
bevy = "0.19"
rand = "0.10"

# Enable a small amount of optimization in the dev profile.
[profile.dev]
opt-level = 1

# Enable a large amount of optimization in the dev profile for dependencies.
[profile.dev.package."*"]
opt-level = 3

[profile.bench]
debug = true
